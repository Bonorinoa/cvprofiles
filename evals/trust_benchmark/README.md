# Trust benchmark lane — recon only

**Status:** Recon-only, de-scoped 2026-08-09.

This folder originally held a recon plan for an SCA2-adapter-vs-persona-prompt
comparison on cultural preferences (generalized trust + GPS dimensions). The
plan lives at `PLAN.md` and was authored as a feasibility study for the SCA2
position paper (`~/Desktop/Github_Repositories/SCA2_PofW/misc/position_paper/`).

## Why nothing is here

The SCA2 lane was de-scoped from cvprofiles on 2026-08-09. The plan is retained
for SCA2-author reference only. **Do not implement, freeze, or run anything in
this folder under the cvprofiles banner.** Any execution must happen inside the
SCA2 repo, with its own dated amendment and freeze.

## The active cvprofiles empirical lane

The cvprofiles flagship empirical example is the **WVS/GPS patience
application** under `evals/wvs_gps_preferences/`, with frozen inputs committed
there and an end-to-end input-builder tutorial at
`tutorials/cvprofiles_wvs_gps_inputs.ipynb`. Two-resolution extensions live at
`evals/wvs_gps_two_resolution/`. Historical H5 Trust evidence lives at
`evals/h5_trust/` (re-graded to regression witness 2026-08-07).

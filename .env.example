# cvprofiles environment variables
# Core engine needs NO secrets. Frozen scores + YAML configs suffice.
#
# Optional upstream *scoring recipes* (outside the engine) may need keys later.
# Never import optional scorer env vars from identify/report code paths.

# Example placeholders only — not used by MVP engine:
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# HF_TOKEN=

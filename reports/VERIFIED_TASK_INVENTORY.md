# VERIFIED_TASK_INVENTORY — cvprofiles v3.0.0 (paper-driven)

**Date:** 2026-08-07 · **Prepared by:** cvprofiles lead engineer · **Status:** verified against repo + `reports/FINAL_ENGINEERING_REPORT.md` + `reports/math_spec.md` + `docs/16` + `docs/17`
**Note:** the prior session's copy was not on disk and not found in session history; this inventory is reproduced from the grounded inputs (the "dissect" step), with every claim re-verified against the working tree. **Rev 2 (2026-08-07):** Augusto redirect — H5 Trust deprecated as the demo lane; v3 flagship is the IVS/Tao cultural-values evaluation (T21–T33).

**Posture:** package `2.0.1a1` (dev), `2.0.0` published on PyPI 2026-08-06 (tag `v2.0.0` @ `6abb6e4`); `v0.1` immovable @ `fb62b48bcb704f60eee7d6641ed0a344eb72bfda`; docs/16 `protocol-v1-synth-provisional` with §8 amendment opening the empirical box for H5 Trust only; H5 n=35 run is **preliminary paper-facing evidence with demotion pending Gate A (T24/T05e)** — no longer the v3 headline.

**Authority:** Augusto owns construct, unit/universe, scores, menu, R/anchors/θ/δ/β, claims, release (AGENTS.md:31-35). Agent authors oracle networks for synthetic DGPs only (AGENTS.md:37). No empirical network is authored by the engineer in this plan.

---

## A. Verified baseline facts (grounding)

| # | Fact | Verified in | Status |
|---|---|---|---|
| F1 | Version `2.0.1a1`; preimage includes `package_version` | pyproject.toml:3; freeze.py:17-20 | OK |
| F2 | `v0.1` peels to `fb62b48…` | git | OK (immovable lock) |
| F3 | Battery: ruff, mypy strict, pytest, import-graph, wheel-smoke in CI | AGENTS.md:93-99; ci.yml | OK (222 logged, docs/12:936) |
| F4 | Bootstrap recomputes M\* and [L,U] per replicate; empty/degenerate counted; band = percentile over non-empty | src/cvprofiles/inference/bootstrap.py:1-18,59-147 | OK — the seed of WP1 |
| F5 | Freeze preimage keys: beta_hash, config, delta, n_boot, network_hash, package_version, schema_version, scores_hash, seed; grids/anchors excluded | freeze.py:17-25 | OK |
| F6 | Restriction registry implemented: `corr_min`, `corr_sign`, `mean_order`, `rank_agree`; `stability` schema-only fail-loud | slacks.py:59-108,111; schemas/network.py:11-17 | OK |
| F7 | β registry: `corr_y`, `ols_coef` implemented; `diff_means` schema-only | identify/beta_fn.py:53-73 | OK |
| F8 | H5 n=35 evidence: M\*={m_trust_general, m_trust_in_group}, [L,U]=[0.371,0.624], FA=0, cold H4; θ-grid empties λ≥1.5; bootstrap band [0.174,0.752], 17.5% empty | reports/summaries/h5_trust_evidence_summary.json; docs/16:133 | OK — historical evidence; **demotion pending Gate A (T24/T05e)** |
| F9 | Doc tension: docs/17:40 says WGI `rq`; manifest + docs/12 record `rl` | docs/17_H5_Trust_Design.md:40 vs :100; docs/12 | **STALE — fix in P0** |
| F10 | Doc tension: docs/16:5 baseline `1.1.0a1` (now `2.0.1a1`) | docs/16_Paper_Protocol_Freeze.md:5 | **STALE — fix in P0** |
| F11 | ROADMAP v3 box "to be drafted" | docs/ROADMAP.md:32 | **STALE — wire in P0** |
| F12 | METHODOLOGY names `monotone_*` as known registry gap; `stability` marked schema-only | docs/METHODOLOGY.md:42,51 | OK — WP3 closes |
| F13 | D1 DECIDED (2026-08-07, Augusto): coverage = additive-but-mandatory reporting layer; headline [L,U] unchanged; no freeze-preimage change; dated docs/12 entry + docs/16 amendment text still required | FINAL_ENGINEERING_REPORT.md:44; math_spec §2c, §6.8 | **Dated locks MISSING — P1** |

## B. Work items (WP → task → scope → governance → verification)

Legend: **Governance** — A = agent-executable, O = Augusto decision/sign-off. **Exit** = the item's exit criterion.

| ID | WP | Task | Files | Governance | Verification | Exit |
|---|---|---|---|---|---|---|
| T01 | P0 | Fix docs/17 `rq`→`rl` (row + any other stale WGI code) | docs/17 | A (transcription fix, docs/12 precedent) | grep `rq`/`rl` in docs/17; battery | No `rq` for rule_of_law; battery green |
| T02 | P0 | Re-baseline docs/16 package baseline to `2.0.1a1` + release posture line | docs/16:5-7 | A (current-state literal) | grep `1.1.0a1` in docs/16 | Baseline == pyproject |
| T03 | P0 | Wire ROADMAP v3 scope box + manifest block (v3_0, reading order) | docs/ROADMAP.md, docs/PROJECT_MANIFEST.md, docs/README.md, AGENTS.md truth table | A | doc-map grep; `git diff --check` | All 5 wiring points updated in one commit |
| T04 | P1 | Draft dated docs/12 entry: D1 semantics lock (coverage = additive-but-mandatory; no preimage change; headline unchanged; α default 0.10, κ=2, honest "uncertainty band" language) | docs/12 (append) | **O — Augusto signs text** | docs/12 entry dated; consistency with math_spec §2c | Entry present + Augusto-approved |
| T05 | P1 | Draft docs/16 amendment (§8-pattern): (a) coverage layer mandated for v3 headline reporting; (b) holdout stage split changes "stated network" semantics; (c) registry additions `monotone_rank`, `corr_zero` (two-sided) as measurement-theory positions; (d) **IVS empirical-box clause opening the cultural-values evaluation (supersedes H5 as the v3 headline)**; (e) **H5 close-out clause**; (f) **leakage claim boundary: "country not provided in the evaluation protocol" is a process claim the engine flags, never proves (parallel to `pre_data` anchors; the T30 prompt-template + score-level audit enforces it)**; list non-authorizations explicitly | docs/16 (append §9/§10) | **O — Augusto signs text** | amendment dated; docs/16 status consistent | Amendment present + approved; semantics locks precede code |
| T06 | P2 | WP1 coverage layer: `CoverageResult` dataclass + `coverage.py` (or extend bootstrap) — per-side α/2 quantiles, Bonferroni-joint labeling, empty-rate headline, boundary attribution (margin ≤ κ·SE), conservative projection cross-check | src/cvprofiles/inference/coverage.py; tests/test_coverage.py | A (post-T05) | TDD RED→GREEN; MC nominal-rate fixture; determinism under seed; empty-rate counting; battery | CoverageResult + tests green; semantics per math_spec §2c |
| T07 | P2 | Wire coverage into pipeline + CLI (`--inference coverage --alpha`); REPORT gains Coverage section (JSON + HTML); exit 0 on empty M\* | identify/pipeline.py (or composition), cli.py, report/pipeline.py, templates | A | CLI contract tests (JSON stdout, stderr crumbs); empty-path render (format(None) guard); battery | CLI + report surface green; empty path renders |
| T08 | P2 | Worked example for coverage: rerun on the package's **existing self-contained mini-fixture** (`reports/runs/v1_1_package_synth/fixture/scores.csv` + `network.yaml`) with a boundary measure added — no artifact dependency on P5a's T22 slice; **no new empirical claims; H5 n=35 coverage numbers are regression/illustrative only, never demo evidence** | runs (ignored), eval-log row, report panel | A (numbers are illustrative, not paper claims) | freeze-core equality vs the same synthetic bundle; band reproduces under fixed seed | Worked example documented in docs/13 as directional |
| T09 | P3 | WP2 schema: restriction-level `stage: select\|holdout` (default select; backward compatible) | schemas/network.py; schemas tests | A (post-T05b) | RED (no stage field) → schema → GREEN; existing networks parse identically | stage field parses; old networks unchanged |
| T10 | P3 | WP2 pipeline: bind both stages; admit on R_select only; REPORT three blocks (selection result; holdout slacks per survivor; holdout verdict as named findings, not errors). **Report-JSON contract: the three-block shape must stay backward-compatible with the existing report.json consumers (`tools/run_many.py`, `tools/verify_*.py`, `reports/summaries/` readers) — additive keys, no renamed fields; versioned if the shape must change** | restrict/pipeline.py, identify/pipeline.py, report/pipeline.py, templates | A | TDD: holdout-fail survivors are findings (exit 0), not errors; empty R_select exit 0; existing report-JSON contract tests still pass | Three-block report green; exit-0 contract; report schema compatible |
| T11 | P3 | WP2 docs: METHODOLOGY notation + registry table (stage semantics); USER_GUIDE pattern; docs/12 dated entry | docs/METHODOLOGY.md, docs/USER_GUIDE.md, docs/12 | A | doc/code agreement grep | Docs describe shipped semantics |
| T12 | P4 | WP3 evaluator `monotone_rank`: slack = sign·Spearman(m,V_cont) − θ; reuse rank path. **Fit note (math-first): the Inglehart–Welzel axes are PCA axes, not a monotone-in-covariate restriction — `monotone_rank` is only used by the IVS lane if the network restricts on a continuous covariate (e.g. "self-expression ↑ with GDP per capita"); otherwise the axis restriction is structural and does not need this evaluator. Gate A (T26 math note) records which.** | slacks.py, schemas/network.py, tests/test_monotone_rank.py | **O — registry position signed in T05c** | fixture → RED → evaluator → GREEN; hand-computed golden; migrate unimplemented-example test if needed | Evaluator green; registry table updated; fit decision recorded in math note |
| T13 | P4 | WP3 evaluator `corr_zero`: slack = θ − \|Corr(m,V)\| (two-sided form) | slacks.py, schemas/network.py, tests/test_corr_zero.py | **O — registry position signed in T05c** | fixture → RED → evaluator → GREEN; hand-computed golden | Evaluator green; two-sided semantics documented |
| T14 | P4 | WP3 `stability`: keep schema-only fail-loud unless the paper's evidence profile demands a reliability surface (decision recorded) | (no code unless decided) | O — defer decision | n/a (fail-loud retained) | Decision recorded in docs/12 |
| T15 | P5 | ~~WP4 scaffold: extend H5 Trust lane~~ **SUPERSEDED 2026-08-07 (Augusto redirect) → T21.** H5 Trust lane stays as historical design lock (docs/17); v3 flagship lane is the IVS/Tao cultural-values evaluation | — | — | — | — |
| T16 | P5 | ~~WP4 run~~ **SUPERSEDED 2026-08-07 → T25.** n=35 H5 evidence remains historical/preliminary, not the v3 headline | — | — | — | — |
| T21 | P5a | **WP4a IVS cultural-values lane (Tao et al. (2024)-style evaluation).** Augusto-authored: scores/menu from IVS (Joint EVS/WVS 2017–2022 v5.0, 92 countries = 36 EVS + 66 WVS; WVS7 read-authorized in SCA2 folder; Joint file free/no registration, standalone EVS ZA7500 is GESIS-registration-gated), Inglehart–Welzel axes as target (varimax PCA of the 10 canonical items, human loadings frozen), network, θ, δ, β, stage split, holdout countries. Agent scaffolds: design-doc template, schema smoke on dummy IVS-shaped scores, data-acquisition runbook. **Frozen PCA loadings: model outputs projected on the same human loadings. "Replication" is reframed per T31: same evaluation setup, model families chosen under policy — never their exact numbers** | docs/18_IVS_Cultural_Map.md (new design doc), evals/ivs_cultural/ data, docs/16 amendment (Gate A) | **O — Augusto authors network/scores/loadings/stage/holdout; one-off delegation possible, never silent authorship** | transcription audit (item codes match pinned network; loadings match human fit); schema smoke on synthetic IVS-shaped scores | Design locked + transcription audited |
| T22 | P5a | **WP4a teaching lane (the walkthrough).** Teach Augusto to author the four inputs and read outputs: worked notebook on a **synthetic IVS-shaped slice** (5–8 pseudo-countries, no real-data dependency — real IVS data only after Gate B's run decision); published tutorial re-run on the frozen lane post-T25 is illustrative only. **Item columns mirror the 10 Inglehart–Welzel codes (A008, A165, E018, E025, F063, F118, F120, G006, Y002, Y003 — per Tao et al. §4.1; to be dictionary-verified against the actual Joint file at the T21 transcription audit)** | tutorials/ivs_cultural_map_tutorial.ipynb + tools/build_tutorials.py | A (post-Gate A; before Gate B design is finalized — the walkthrough informs the network design) | wheel smoke (fresh-venv `pip install` notebook run); nbconvert execute exit 0; teaching assertions green | Notebook reproducible under wheel; teaching assertions green |
| T23 | P5b | **WP2 applied to IVS: country-level holdout.** Same frozen network + same β; **units-split design: select/admit on train-country scores, then evaluate the survivors' slacks + verdict on held-out-country scores** (the paper's falsifiable core as currently phrased). **CRUX — Gate A (T26 math note) decides which holdout is the paper's core:** (a) restriction-level `stage: select\|holdout` (the T09/T10 machinery — same units, R_select predicts R_holdout compliance), or (b) country-level units-split (train-country selection → held-out-country verdict — needs a units-split workflow the P3 scope does not fully specify). The other is optional; both are implementable; they are not the same TDD | identify/pipeline.py, report/pipeline.py, templates (stage split from T09/T10) + units-split composition (if (b) is core) | A (after Gate A's holdout-semantics math note) | TDD: holdout-failure survivors are named findings (exit 0), not errors; dummy-country split reproduces selection + verdict on held-out units | Three-block report green on IVS-shaped inputs; units-split composition green if (b) |
| T24 | P6 | **H5 lane close-out.** Dated reversal: docs/12 entry; docs/17 status → historical design lock; docs/16 amendment noting H5 is no longer the v3 headline; docs/13 row re-grading n=35 from "preliminary paper-facing evidence" to historical/regression witness. **Timing: the docs/13 re-grade moves to immediately after Gate A (T05e signed) to close the P1→P6 contradiction window; the P6 task is the full mechanical reversal + battery** | docs/12, docs/16, docs/17, docs/13 | **O — Augusto signs the re-grade at Gate A** (re-grading docs/13 evidence claims is a scientific-claims change, not a transcription fix) | dated entries present; no live doc claims H5 is the v3 headline; docs/13 re-grade executed right after Gate A | H5 demoted consistently; battery green |
| T25 | P5b | **IVS run + paper-lock checkpoint.** Frozen scores + pinned seed + version + frozen loadings; dev gate (FA=0, cold H4, empty honesty, positive control: known-valid items admissible); independent audit (verify pattern) exit 0; docs/13 row only after audit | runs; proof_summary; reports/summaries/ivs_cultural_evidence_summary.json | **O — run decision + paper lock** | verify_h5_trust-style auditor exit 0; positive-control gate green | Audit exit 0 + Augusto's run/go decision |
| T26 | P1 | **Gate A math decisions: (a) distance-vs-correlation for the IW map; (b) holdout semantics.** (a) Tao's metric is 2D Euclidean distance to benchmark — current registry cannot express it. Options: (i) `map_distance` as new β functional (my recommendation — varimax-orthogonal PCA ⇒ distance is the natural target), or (ii) per-axis `corr_min` + distance as diagnostic. **Frozen loadings provenance also Augusto's call: reuse Tao's published loadings verbatim incl. the PC2′ = 1.61·PC2 − 0.01 rescaling (deterministic given their correlation matrix — makes the lane a within-their-framework evaluation; my recommendation for the headline), or fresh fit (an empirical modeling claim — robustness appendix only).** (b) **Holdout semantics: which holdout is the paper's falsifiable core — restriction-level `stage` split (T09/T10 machinery) or country-level units-split (T23)? Record the choice in a dated math note before P3 TDD** | schemas/beta.py, identify/beta_fn.py (if (a-i)); math_spec note | **O — Gate A sign-off on β-functional choice + holdout semantics (measurement positions, not engineering)** | math note dated; decisions recorded in docs/12 | Decisions recorded; functional green if (a-i) |
| T27 | P1 | **Gate A non-goal reopen: adapter training.** DPO adapter training = "foundation-model training" (AGENTS.md explicit non-goal). Options: (a) dated reopen for upstream score-generation harness ONLY (never in src/), or (b) defer adapters; **the v3 harness still runs open-weight prompt-based baselines (AGENTS.md-permitted)** | AGENTS.md non-goal list, docs/12 | **O — Augusto decides reopen vs defer** | dated docs/12 entry; AGENTS.md updated if reopened | Decision recorded |
| T31 | P1 | **Gate A policy reopen: proprietary-API non-goal.** IVS score generation needs LLM outputs; AGENTS.md "proprietary APIs for paper-reproducible work" conflicts with GPT-4o/Claude-class scoring for a Nature/PNAS checkpoint (model deprecation ⇒ numbers not reproducible). Options: (a) open-weight local models (reproducible; recommendation), or (b) dated reopen for pinned proprietary-API snapshots with fully frozen outputs. **"Replicating Tao et al." is reframed as "same evaluation setup, model families chosen under this policy" — never their exact numbers with different models. Coupled with T27 (model-family policy + adapter policy decided together at Gate A)** | AGENTS.md non-goal list, docs/12 | **O — Augusto decides (joint with T27)** | dated docs/12 entry; AGENTS.md updated if reopened; math_spec note on model-family boundary | Decision recorded |
| T32 | P1 | **Long-lead IVS data acquisition.** Download Joint EVS/WVS 2017–2022 v5.0 (92 countries = 36 EVS + 66 WVS; free, no registration — avoids re-harmonizing WVS7 + EVS); **raw files stay out of git (gitignored + sha256 manifest) — mirroring raw survey data into the repo may violate GESIS redistribution terms even though download is free; only derived scores enter the repo, and their publication license is checked at Gate B**. WVS7 already read-authorized in the SCA2 folder. Standalone EVS ZA7500 is GESIS-registration-gated — use the Joint file. **Exit includes item-code verification: confirm the 10 PROVISIONAL IW codes (A008, A165, E018, E025, F063, F118, F120, G006, Y002, Y003 — from Tao et al. §4.1) against the Joint EVS/WVS dictionary at acquisition time; T22's synthetic mirror uses the verified codes; T33 re-checks them as a hard Gate B item** | evals/ivs_cultural/data (derived scores), provenance manifest (raw gitignored) | A (data acquisition, no scientific claims) | file presence + sha256 + documented version + license note for derived-score publication + item-code dictionary verification | Data mirrored with provenance + license note + verified item codes; manifest recorded |
| T33 | P5b | **WP4b IVS independent auditor.** `tools/verify_ivs_cultural.py` (read-only verifier, pattern: `verify_h5_trust.py`): strict JSON (reject NaN/Infinity; null only for structurally empty range); FA=0; freeze-core equality across two cold runs; provenance incl. frozen loadings + model-family policy; positive-control gate (known-valid items admissible); item-code dictionary verification (the 10 IW codes vs the actual Joint file). Structural audit ≠ paper acceptance; Gate B run decision remains Augusto's | tools/verify_ivs_cultural.py + tests | A (agent-authored verifier; exit is Augusto's Gate B decision) | verifier exit 0 on a dummy IVS-shaped proof; strict-loader tests (reject NaN/Inf); battery | Auditor green; exit 0 is a hard Gate B entry condition |
| T28 | P2 | **WP1b additive: p̂_m = per-measure admission probability** = fraction of bootstrap replicates where m ∈ M*. Descriptive selection probability, not a coverage statement (inherits WP1 selection coupling) | inference/coverage.py (or bootstrap.py), tests | A (post-Gate A) | TDD: fixture with boundary measure → RED → compute → GREEN; empty replicates handled (denominator = non-empty, matching band; null band ⇒ p̂_m null + note) | p̂_m green; semantics in math_spec note §2c.4 |
| T29 | P2 | **WP1c additive: MTMM trait×method panel.** trait = construct axis (self-expression, secular-rational), method = model adapter/prompt baseline; Campbell–Fiske-style correlation panel as additive report diagnostic — NOT a new core module | tools/mtmm.py, report/pipeline.py, templates, tests | A (after T26; MTMM is a reporting diagnostic, no engine state change) | fixture 2 traits × 2 methods, TDD RED→GREEN; empty M* ⇒ "no admissible measures" string, exit 0 | Panel green; fail-loud evaluator registry intact |
| T30 | P5a | **WP4b upstream score-generation harness scaffold** (model calls, prompt templates, projection on frozen loadings; DPO adapter stub ONLY if T27 reopens). Lives in `evals/ivs_cultural/`; **no LLM client in src/ import graph** (AST-tested). **Default posture: open-weight prompt-based baselines are already AGENTS.md-permitted; only adapters (T27) and proprietary APIs (T31) need dated reopens** | evals/ivs_cultural/{project,score,harness}.py | A (scaffold after Gate A's T05d; DPO adapter stub gated on T27) | schema smoke on dummy projected scores; `src/` import-graph still LLM-free (AST test); leakage audit + snapshot pinning | Harness scaffold green; import-graph lock intact |
| T17 | P6 | Docs close-out: METHODOLOGY inference stance (§5), ARCHITECTURE module map, ROADMAP, MANIFEST, docs/13 rows (authorized only) | docs/* | A | doc/code agreement; battery | All docs current; no stale version labels |
| T18 | P6 | v3.0.0 gate battery: ruff, mypy strict, pytest ≥223, all proof verifiers (3) exit 0, `git diff --check`, `v0.1` peel, import-graph, wheel-smoke | CI + local | A | full battery | All green |
| T19 | P6 | Atomic bump `2.0.1a1 → 3.0.0` + golden refresh + all literals (pyproject, `__init__`, uv.lock, tests, CI CLI-smoke, README/AGENTS posture, manifest dev_version) | pyproject, src, tests, .github, docs | A (mechanical) | bump checklist; golden refreshed in same commit; battery | Version consistent; golden run_id reported |
| T20 | P6 | Release decision: tag + PyPI upload | git tag, PyPI | **O — Augusto** (token never in agent session) | post-publish verification chain (JSON API + sha256 + fresh-venv notebook) | Published + independently verified |

## C. Dependency graph (order-critical)

```
P0 (T01-T03) ──► P1 locks + math + policy (T04-T05, T26, T27, T31, T24 sign-off) ── GATE A (Augusto, 7 decisions)
   P1 ──► P2 (T06-T08, T28, T29)    [coverage + p̂_m + MTMM panel; fully parallel, merge before P6]
   P1 ──► P3 (T09-T11)              [holdout workflow]
   P1 ──► P4 (T12-T14)              [evaluators — monotone_rank + corr_zero (dual-purpose: discriminant + IW axis separation)]
   P3 ∥ P4                          [parallel branches given Gate A; both feed P5a; neither blocks the other]
   P2, P3, P4 ──► P5a (T21 design, T22 teaching walkthrough, T30 harness)
   IVS data acquisition (T32, Joint EVS/WVS v5.0) starts in parallel at P1 (long-lead)
   P5a ──► P5b (T23 holdout-applied + T33 auditor + T25 run + audit + paper lock) ── GATE B (Augusto)
   P5b ──► P6 (T17-T20, T24 H5 close-out) ── GATE C (Augusto)
```

**Ordering constraint:** T22 (teaching walkthrough) runs **before** the final network authorship (T21) — the walkthrough informs the design; it uses synthetic IVS-shaped scores (no real-data dependency), and the published tutorial is re-run under the v3 wheel on the frozen lane after T25.

**Critical path to the paper's headline evidence:** P0 → P1 (Gate A) → max(P3 holdout, P4 evaluators) → P5a (design + teaching + harness) → P5b (run + audit + paper lock) → P6 (release). P3 and P4 are **parallel** branches given Gate A — serializing them is a scheduling choice, not a dependency. WP1 (P2) is fully parallel (merge before P6): it's the long pole for the *methods paper's* inference claim but does not block the empirical run. No engine task starts before its semantics lock (T04/T05/T26).

## D. Explicit non-goals for this plan

- No empirical network authored by the engineer (AGENTS.md:37) — T21 is Augusto-owned.
- No new empirical claims from the H5 n=35 lane — deprecated to historical/regression witness (T24, Augusto-signed).
- No freeze-preimage change for coverage (D1) — headline [L,U] semantics unchanged.
- No `stability` evaluator unless the paper demands it (T14).
- No LLM in engine/import graph — T30 harness lives in `evals/`, never `src/` (AST-tested).
- **Adapter training (DPO/LoRA) is AGENTS.md "foundation-model training" non-goal — reopen is Augusto's Gate A decision (T27).** If reopened, it is an upstream score-generation harness only; no model weights ship in the package.
- No engine-level multi-construct joint admissibility (out of scope, archive lock).
- **Disposed (considered, not absorbed):** IRT engine inside `src/` (stays an upstream scoring tutorial — model-based scoring inside the engine violates score-agnosticism); sensemakr/OVB bounds integration (orthogonal question — stays a tutorial); "PartialIdentificationSolver" module (that's the existing IDENTIFY state — no new module); declarative Nomological DSL (the YAML schema already is the DSL *shape*; Gemini's two-sided interval-bound semantic is real and maps to `corr_zero` + `corr_min` composition — a Python-fluent builder is optional sugar, not a new core).
- **Process-claim boundary:** "country never mentioned" is a *process* commitment the engine cannot verify (parallel to `pre_data` θ-anchors). The engine enforces a protocol flag; the prompt-template audit lives in the harness (T30).
